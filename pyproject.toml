[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "djust-monitor"
version = "0.1.0"
description = "Error tracking and monitoring client for djust applications"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "django>=4.2",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-django",
]

[tool.hatch.build.targets.wheel]
packages = ["src/djust_monitor"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
